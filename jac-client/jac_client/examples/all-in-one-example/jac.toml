[project]
name = "all-in-one-example"
version = "1.0.0"
description = "Jac client application: all-in-one-example"
entry-point = "src/app.jac"

[plugins.client]
# Vite bundler configuration (optional overrides)
# vite.plugins = []
# vite.build = {}
